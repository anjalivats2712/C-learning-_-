#include <stdio.h>

int main(){
    int a;
    printf("enter the number");
    scanf("%d",&a);
    if(a==1){
        printf("the number is %d",a);

    }
    else if(a==2){
        printf("the number is %d",a);

    }
    else if(a==3){
        printf("the number is %d",a);
    }
    else{
        printf("the number is not in range");
    }
    return 0;
}