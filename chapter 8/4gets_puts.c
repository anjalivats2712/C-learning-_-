#include <stdio.h>

int main(){
    char str[50];
    gets(str);//takes input
    printf("now we will print the input as output\n");
    puts(str);//prints input
    char name[45];
    printf("enter the name \n");
    gets(name);
    puts(name);
    return 0;

    

     

}